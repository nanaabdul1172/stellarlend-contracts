[package]
name = "cross-asset-test"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["lib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
stellar-lend-common = { path = "../contracts/common" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
